import json\nimport os\nfrom typing import Dict, List, Optional\nfrom datetime import datetime\n\n\nclass TodoManager:\n    \"\"\"To-Do List ম্যানেজমেন্ট সিস্টেম\"\"\"\n    \n    def __init__(self, data_dir=\"data\"):\n        self.data_dir = data_dir\n        if not os.path.exists(data_dir):\n            os.makedirs(data_dir)\n    \n    def _get_user_file(self, user_id: int) -> str:\n        \"\"\"ব্যবহারকারীর টু-ডু ফাইল পান\"\"\"\n        return os.path.join(self.data_dir, f\"todo_{user_id}.json\")\n    \n    def _load_todos(self, user_id: int) -> List[Dict]:\n        \"\"\"টু-ডু লিস্ট লোড করুন\"\"\"\n        file_path = self._get_user_file(user_id)\n        if os.path.exists(file_path):\n            try:\n                with open(file_path, 'r', encoding='utf-8') as f:\n                    return json.load(f)\n            except:\n                return []\n        return []\n    \n    def _save_todos(self, user_id: int, todos: List[Dict]):\n        \"\"\"টু-ডু লিস্ট সেভ করুন\"\"\"\n        file_path = self._get_user_file(user_id)\n        with open(file_path, 'w', encoding='utf-8') as f:\n            json.dump(todos, f, ensure_ascii=False, indent=2)\n    \n    def add_todo(self, user_id: int, task: str, priority: str = \"normal\") -> str:\n        \"\"\"\n        নতুন টাস্ক যোগ করুন\n        \n        Args:\n            user_id: ব্যবহারকারী ID\n            task: টাস্কের বর্ণনা\n            priority: অগ্রাধিকার (low, normal, high)\n        \n        Returns:\n            সাফল্যের বার্তা\n        \"\"\"\n        todos = self._load_todos(user_id)\n        \n        new_todo = {\n            'id': len(todos) + 1,\n            'task': task,\n            'priority': priority,\n            'completed': False,\n            'created_at': datetime.now().strftime('%Y-%m-%d %H:%M:%S')\n        }\n        \n        todos.append(new_todo)\n        self._save_todos(user_id, todos)\n        \n        priority_emoji = {'low': '🟢', 'normal': '🟡', 'high': '🔴'}\n        return f\"✅ টাস্ক যোগ হয়েছে!\\n{priority_emoji.get(priority, '🟡')} {task}\"\n    \n    def get_all_todos(self, user_id: int) -> str:\n        \"\"\"\n        সব টু-ডু দেখুন\n        \"\"\"\n        todos = self._load_todos(user_id)\n        \n        if not todos:\n            return \"📋 আপনার কোনো টাস্ক নেই। `/todo add` দিয়ে একটি যোগ করুন!\"\n        \n        todo_text = \"📋 **আপনার টু-ডু লিস্ট:**\\n\\n\"\n        \n        incomplete = [t for t in todos if not t['completed']]\n        completed = [t for t in todos if t['completed']]\n        \n        if incomplete:\n            todo_text += \"**📌 করার বাকি:**\\n\"\n            for todo in sorted(incomplete, key=lambda x: {'high': 0, 'normal': 1, 'low': 2}.get(x.get('priority', 'normal'))):\n                priority_emoji = {'low': '🟢', 'normal': '🟡', 'high': '🔴'}\n                todo_text += f\"{priority_emoji.get(todo.get('priority', 'normal'), '🟡')} **#{todo['id']}** {todo['task']}\\n\"\n        \n        if completed:\n            todo_text += \"\\n**✅ সম্পন্ন:**\\n\"\n            for todo in completed:\n                todo_text += f\"✔️ **#{todo['id']}** ~~{todo['task']}~~\\n\"\n        \n        return todo_text\n    \n    def mark_complete(self, user_id: int, task_id: int) -> str:\n        \"\"\"\n        টাস্ক সম্পন্ন চিহ্নিত করুন\n        \"\"\"\n        todos = self._load_todos(user_id)\n        \n        for todo in todos:\n            if todo['id'] == task_id:\n                todo['completed'] = True\n                todo['completed_at'] = datetime.now().strftime('%Y-%m-%d %H:%M:%S')\n                self._save_todos(user_id, todos)\n                return f\"✅ টাস্ক #{task_id} সম্পন্ন করা হয়েছে!\\n📝 {todo['task']}\"\n        \n        return f\"❌ টাস্ক #{task_id} পাওয়া যায়নি\"\n    \n    def delete_todo(self, user_id: int, task_id: int) -> str:\n        \"\"\"\n        টাস্ক ডিলিট করুন\n        \"\"\"\n        todos = self._load_todos(user_id)\n        \n        for i, todo in enumerate(todos):\n            if todo['id'] == task_id:\n                task_text = todo['task']\n                todos.pop(i)\n                # ID পুনর্নির্ধারণ করুন\n                for idx, t in enumerate(todos, 1):\n                    t['id'] = idx\n                self._save_todos(user_id, todos)\n                return f\"🗑️ টাস্ক ডিলিট হয়েছে!\\n📝 {task_text}\"\n        \n        return f\"❌ টাস্ক #{task_id} পাওয়া যায়নি\"\n    \n    def get_stats(self, user_id: int) -> str:\n        \"\"\"\n        টু-ডু স্ট্যাটিস্টিকস পান\n        \"\"\"\n        todos = self._load_todos(user_id)\n        \n        total = len(todos)\n        completed = len([t for t in todos if t['completed']])\n        pending = total - completed\n        high_priority = len([t for t in todos if t['priority'] == 'high' and not t['completed']])\n        \n        stats_text = f\"\"\"📊 **আপনার স্ট্যাটিস্টিক্স:**\n\n📋 **মোট টাস্ক:** {total}\n✅ **সম্পন্ন:** {completed}\n📌 **বাকি:** {pending}\n🔴 **জরুরি:** {high_priority}\n\n**সম্পূর্ণতা:** {f'{int((completed/total)*100)}%' if total > 0 else '0%'}\n\"\"\"\n        return stats_text\n"