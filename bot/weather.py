import requests\nfrom typing import Dict, Optional\n\n\nclass WeatherManager:\n    \"\"\"আবহাওয়া তথ্য ব্যবস্থাপনা\"\"\"\n    \n    # আপনার নিজের API key ব্যবহার করতে পারেন\n    # https://openweathermap.org/api থেকে ফ্রি key পাবেন\n    API_KEY = \"8d4fb5b93d4af21d66a2948710284366\"  # Demo key\n    BASE_URL = \"https://api.openweathermap.org/data/2.5\"\n    \n    @staticmethod\n    def get_current_weather(city: str) -> Optional[Dict]:\n        \"\"\"\n        বর্তমান আবহাওয়া পান\n        \n        Args:\n            city: শহরের নাম\n        \n        Returns:\n            আবহাওয়ার ডেটা বা None\n        \"\"\"\n        try:\n            url = f\"{WeatherManager.BASE_URL}/weather\"\n            params = {\n                'q': city,\n                'appid': WeatherManager.API_KEY,\n                'units': 'metric'\n            }\n            \n            response = requests.get(url, params=params, timeout=5)\n            response.raise_for_status()\n            \n            data = response.json()\n            return data\n        \n        except requests.exceptions.RequestException as e:\n            return None\n    \n    @staticmethod\n    def format_weather(weather_data: Dict) -> str:\n        \"\"\"\n        আবহাওয়ার তথ্য ফর্ম্যাট করুন\n        \n        Args:\n            weather_data: OpenWeatherMap API ডেটা\n        \n        Returns:\n            ফর্ম্যাট করা টেক্সট\n        \"\"\"\n        if not weather_data:\n            return \"❌ আবহাওয়া তথ্য পাওয়া যায়নি\"\n        \n        try:\n            city = weather_data.get('name', 'Unknown')\n            country = weather_data.get('sys', {}).get('country', '')\n            \n            main = weather_data.get('main', {})\n            temp = main.get('temp', 'N/A')\n            feels_like = main.get('feels_like', 'N/A')\n            humidity = main.get('humidity', 'N/A')\n            pressure = main.get('pressure', 'N/A')\n            \n            weather = weather_data.get('weather', [{}])[0]\n            description = weather.get('description', 'N/A').capitalize()\n            \n            wind = weather_data.get('wind', {})\n            wind_speed = wind.get('speed', 'N/A')\n            \n            # Emoji নির্বাচন\n            emoji = WeatherManager.get_weather_emoji(weather.get('main', ''))\n            \n            weather_text = f\"\"\"{emoji} **{city}, {country}** এর আবহাওয়া\n\n🌡️ **তাপমাত্রা:** {temp}°C\n🤔 **অনুভূত:** {feels_like}°C\n💧 **আর্দ্রতা:** {humidity}%\n🌬️ **বায়ুর গতি:** {wind_speed} m/s\n🔽 **চাপ:** {pressure} hPa\n\n📝 **অবস্থা:** {description}\n\"\"\"\n            \n            return weather_text\n        \n        except Exception as e:\n            return f\"❌ ত্রুটি: {str(e)}\"\n    \n    @staticmethod\n    def get_weather_emoji(weather_type: str) -> str:\n        \"\"\"\n        আবহাওয়ার ধরন অনুযায়ী emoji পান\n        \"\"\"\n        emoji_map = {\n            'Clear': '☀️',\n            'Clouds': '☁️',\n            'Rain': '🌧️',\n            'Drizzle': '🌦️',\n            'Thunderstorm': '⛈️',\n            'Snow': '❄️',\n            'Mist': '🌫️',\n            'Smoke': '💨',\n            'Haze': '🌫️',\n            'Dust': '🌪️',\n            'Fog': '🌫️',\n            'Sand': '🌪️',\n            'Ash': '🌪️',\n            'Squall': '🌪️',\n            'Tornado': '🌪️',\n        }\n        return emoji_map.get(weather_type, '🌍')\n    \n    @staticmethod\n    def get_forecast(city: str) -> Optional[Dict]:\n        \"\"\"\n        ৫ দিনের পূর্বাভাস পান\n        \n        Args:\n            city: শহরের নাম\n        \n        Returns:\n            পূর্বাভাস ডেটা বা None\n        \"\"\"\n        try:\n            url = f\"{WeatherManager.BASE_URL}/forecast\"\n            params = {\n                'q': city,\n                'appid': WeatherManager.API_KEY,\n                'units': 'metric',\n                'cnt': 40  # ৫ দিন = ৪০ records (3 ঘণ্টা প্রতিটি)\n            }\n            \n            response = requests.get(url, params=params, timeout=5)\n            response.raise_for_status()\n            \n            data = response.json()\n            return data\n        \n        except requests.exceptions.RequestException:\n            return None\n    \n    @staticmethod\n    def format_forecast(forecast_data: Dict) -> str:\n        \"\"\"\n        পূর্বাভাস ডেটা ফর্ম্যাট করুন\n        \"\"\"\n        if not forecast_data:\n            return \"❌ পূর্বাভাস তথ্য পাওয়া যায়নি\"\n        \n        try:\n            city = forecast_data.get('city', {}).get('name', 'Unknown')\n            country = forecast_data.get('city', {}).get('country', '')\n            \n            forecast_text = f\"📅 **{city}, {country}** - ৫ দিনের পূর্বাভাস\\n\\n\"\n            \n            # প্রতিটি দিনের প্রথম রেকর্ড নিন\n            list_data = forecast_data.get('list', [])\n            daily_forecasts = {}\n            \n            for item in list_data:\n                dt_txt = item.get('dt_txt', '')\n                date = dt_txt.split(' ')[0]\n                \n                if date not in daily_forecasts:\n                    daily_forecasts[date] = item\n            \n            count = 0\n            for date, item in sorted(daily_forecasts.items()):\n                if count >= 5:\n                    break\n                \n                main = item.get('main', {})\n                weather = item.get('weather', [{}])[0]\n                temp = main.get('temp', 'N/A')\n                description = weather.get('description', 'N/A').capitalize()\n                emoji = WeatherManager.get_weather_emoji(weather.get('main', ''))\n                \n                forecast_text += f\"{emoji} **{date}**: {temp}°C - {description}\\n\"\n                count += 1\n            \n            return forecast_text\n        \n        except Exception as e:\n            return f\"❌ ত্রুটি: {str(e)}\"\n